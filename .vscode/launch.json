{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Hugo Server",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:1313",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start Hugo Server"
        }
    ]
}